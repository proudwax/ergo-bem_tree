ctx = '.cart-info'
{ctx} {
	display: flex;
	justify-content: flex-end;

	&__cart{
		margin-right: 1rem;
	}

	&__info{
		display: flex;
		flex-direction: column;

		.icon{
			padding-left: 3px;
		}
	}
}