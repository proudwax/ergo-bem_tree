ctx = '.cart-item'
{ctx} {
	display: flex;
	flex-flow: row nowrap;
	align-items: center;
	
	&__image{
		width: 50px;
		flex: 0 1 auto;
		
		img{
			width: 100%;
		}
	}
	
	&__name{
		flex: 0 1 auto;
	}
}